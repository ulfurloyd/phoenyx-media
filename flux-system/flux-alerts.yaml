apiVersion: notification.toolkit.fluxcd.io/v1
kind: Alert
metadata:
  name: flux-alerts
  namespace: flux-system
spec:
  providerRef:
    name: discord

  eventSeverity: error

  eventSources:
    - kind: GitRepository
      name: "*"
    - kind: Kustomization
      name: "*"
