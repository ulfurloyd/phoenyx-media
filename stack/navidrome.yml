services:
  navidrome:
    image: deluan/navidrome
    container_name: navidrome
    user: 1000:1000
    networks:
      - media_net
    ports:
      - "4533:4533"
    volumes:
      - ${CONFIG_BASE_PATH}/navidrome:/data
      - ${MUSIC_LIBRARY_PATH}:/music:ro
    restart: unless-stopped
