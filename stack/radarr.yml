services:
  radarr:
    image: linuxserver/radarr
    container_name: radarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_BASE_PATH}/radarr:/config
      - ${MOVIES_LIBRARY_PATH}:/movies
      - ${DOWNLOADS_PATH}:/downloads
    labels:
      - diun.enable=true
      - dynacat.name=Radarr
      - dynacat.icon=sh:radarr
      - dynacat.url=https://radarr.phoenyxlab.duckdns.org
      - dynacat.description=Movie Acquisition
    restart: unless-stopped
    networks:
      - media_net
    depends_on:
      - gluetun
