services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_BASE_PATH}/jellyfin:/config
      - ${SHOWS_LIBRARY_PATH}:/shows
      - ${MOVIES_LIBRARY_PATH}:/movies
      - ${MUSIC_LIBRARY_PATH}:/music:ro
    labels:
      - diun.enable=true
      - dynacat.name=Jellyfin
      - dynacat.icon=sh:jellyfin
      - dynacat.url=https://jellyfin.phoenyxlab.duckdns.org
      - dynacat.description=Media Library
    restart: unless-stopped
    networks:
      - media_net
    depends_on:
      - gluetun
    devices:
      - /dev/dri:/dev/dri
