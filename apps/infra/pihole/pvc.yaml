apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pihole-config
  namespace: infra
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
