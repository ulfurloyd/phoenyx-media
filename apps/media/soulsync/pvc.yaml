apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soulsync-config
  namespace: media
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
