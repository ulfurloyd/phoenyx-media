apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pinepods-postgres
  namespace: media
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
