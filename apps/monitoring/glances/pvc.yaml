apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glances-config
  namespace: monitoring
spec:
  storageClassName: local-path
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
